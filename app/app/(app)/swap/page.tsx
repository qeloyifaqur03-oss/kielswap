'use client'

import { SwapWindow } from '@/components/swap/SwapWindow'

export default function SwapPage() {
  return (
    <section className="relative z-10 min-h-screen flex items-center justify-center px-4 sm:px-6 md:px-8 py-20">
      <SwapWindow />
    </section>
  )
}
