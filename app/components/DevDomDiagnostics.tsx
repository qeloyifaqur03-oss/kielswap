'use client'

// DevDomDiagnostics - disabled (no patching, no interference)
// Can be removed entirely if not needed
export function DevDomDiagnostics() {
  // No patching, no interference with DOM APIs
  return null
}
